

const markMass = 78;
const markHeight = 1.69;

const johnMass = 120;
const johnHeight = 1.95;

const markBMI = markMass / (markHeight * markHeight);
const johnBMI = johnMass / johnHeight ** 2;

// const markHigherBMI = markBMI > johnBMI;

// console.log(`Marks BMI is ${Math.round(markBMI)} and John's BMI is ${Math.round(johnBMI)}. Is Mark's BMI higher than Johns: ${markHigherBMI}`)

// if (markBMI > johnBMI) {
//     console.log(`Marks BMI is ${Math.round(markBMI)} and John's BMI is ${Math.round(johnBMI)}. Mark's BMI higher than John's!`)
// } else {
//     console.log(`Marks BMI is ${Math.round(markBMI)} and John's BMI is ${Math.round(johnBMI)}. John's BMI higher than Mark's`)
// }