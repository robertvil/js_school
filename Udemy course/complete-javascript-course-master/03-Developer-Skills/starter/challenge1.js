'use strick';

/* Q&A
1. Does this array show only upcoming weathers. Looks that yes
2. Is this date in the right chronological order. It this case it is*/

/* Divide & concur aka divide in sub problems
1. Make a function which loops trough given array
2. Function should extract each number and add to it celsius symbol and to what day it corresponds 
3. Log the result to the console
*/

// const temperatureData1 = [17, 21, 23];
// const temperatureData2 = [12, 5, -5, 0, 4];

// const printForecast = function (temps) {
//   let textConstructor = '... ';

//   for (let i = 0; i < temps.length; i++) {
//     textConstructor += `${temps[i]}℃ in ${i + 1} days ... `;
//   }

//   return textConstructor;
// };

// console.log(printForecast(temperatureData2));
